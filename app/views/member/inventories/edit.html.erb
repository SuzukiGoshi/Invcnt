<% @title = "在庫の編集" %>
<h1><%= @title %></h1>

<div id="generic-form">
	<%= form_with model: @inventory, url: [ :leader, @inventory ] do |f| %>
		<%= render "form", f: f %>
		<div class="buttons">
			<%= f.submit "更新" %>
			<%= link_to "キャンセル", :leader_inventories %>
		</div>
	<% end %>
</div>
